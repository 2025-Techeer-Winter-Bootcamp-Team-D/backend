name: CI/CD

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  ci:
    name: Continuous Integration
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff
          
      - name: Run Ruff
        run: ruff check .

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: timescale/timescaledb:latest-pg16
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
      - name: Run Tests
        env:
          DATABASE_URL: postgresql://postgres:password@localhost:5432/test_db
          REDIS_URL: redis://localhost:6379/0
          DJANGO_SECRET_KEY: test_secret_key
          DEBUG: "True"
        run: |
          python manage.py test

  build-and-push:
    name: Build & Push Docker Images
    needs: [ci, test]
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    permissions:
      contents: read
      packages: write
      
    strategy:
      matrix:
        include:
          - service: backend-core
            context: .
            file: Dockerfile
            image: backend-core
          - service: kis-publisher
            context: .
            file: ./kis-publisher/Dockerfile
            image: kis-publisher
          - service: persistence-worker
            context: .
            file: ./persistence-worker/Dockerfile
            image: persistence-worker

    steps:
      - uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/techeer-winter/${{ matrix.image }}
          tags: |
            type=raw,value=latest
            type=sha,format=long

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.file }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-data:
    name: Deploy Data Layer
    needs: [build-and-push]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    steps:
      - uses: actions/checkout@v4

      - name: Copy deployment files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DATA_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy/data/docker-compose.yml"
          target: "~/techeer-deploy/deploy/data"
          strip_components: 2

      - name: Copy OpenSearch Dockerfile
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DATA_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "opensearch/*"
          target: "~/techeer-deploy/opensearch"
          strip_components: 1

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DATA_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/techeer-deploy/deploy/data
            echo "${{ secrets.ENV_FILE_DATA }}" > .env
            sudo mkdir -p /home/ubuntu/infra-data/opensearch
            sudo chown -R 1000:1000 /home/ubuntu/infra-data/opensearch
            
            docker compose pull
            docker compose up -d --build --remove-orphans
            docker image prune -f

  deploy-app:
    name: Deploy Application Layer
    needs: [deploy-data]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    steps:
      - uses: actions/checkout@v4

      - name: Copy deployment files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.APP_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy/app/*"
          target: "~/techeer-deploy"
          strip_components: 0
          overwrite: true

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.APP_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/techeer-deploy/deploy/app
            echo "${{ secrets.ENV_FILE_APP }}" > .env

            # GHCR 로그인 및 배포
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose pull
            docker compose up -d --build --remove-orphans
            docker image prune -af

      - name: Verify App deployment
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.APP_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "=== 컨테이너 상태 확인 ==="
            docker ps

            echo "=== Django App Health Check ==="
            sleep 10
            curl -f http://localhost/health/ || echo "Django health check failed"

            echo "=== Node Exporter Health Check ==="
            curl -f http://localhost:9100/metrics || echo "Node Exporter health check failed"

            echo "=== cAdvisor Health Check ==="
            curl -f http://localhost:8081/metrics || echo "cAdvisor health check failed"

  deploy-monitoring:
    name: Deploy Monitoring Layer
    needs: [deploy-app]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/develop'
    steps:
      - uses: actions/checkout@v4

      - name: Copy monitoring deployment files
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.MONITORING_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "deploy/monitoring/*"
          target: "~/techeer-deploy"
          strip_components: 0
          overwrite: true

      - name: Deploy Monitoring Stack via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.MONITORING_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd ~/techeer-deploy/deploy/monitoring
            echo "${{ secrets.ENV_FILE_MONITORING }}" > .env

            # prometheus.yml에 IP 주소 치환
            source .env
            sed -i "s/\${APP_PRIVATE_IP}/$APP_PRIVATE_IP/g" prometheus.yml
            sed -i "s/\${DATA_PRIVATE_IP}/$DATA_PRIVATE_IP/g" prometheus.yml

            # Docker Compose로 모니터링 스택 시작
            docker compose pull
            docker compose up -d --build --remove-orphans
            docker image prune -af

      - name: Verify Monitoring Stack
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.MONITORING_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "=== 컨테이너 상태 확인 ==="
            docker ps

            echo "=== Prometheus Health Check ==="
            sleep 15
            curl -f http://localhost:9090/-/healthy || echo "Prometheus health check failed"

            echo "=== Grafana Health Check ==="
            curl -f http://localhost:3000/api/health || echo "Grafana health check failed"

            echo "=== Loki Health Check ==="
            curl -f http://localhost:3100/ready || echo "Loki health check failed"

            echo "=== Tempo Health Check ==="
            curl -f http://localhost:3200/ready || echo "Tempo health check failed"

            echo "=== Alertmanager Health Check ==="
            curl -f http://localhost:9093/-/healthy || echo "Alertmanager health check failed"

            echo "=== Portainer Health Check ==="
            curl -f http://localhost:9000/api/status || echo "Portainer health check failed"